"""
[업데이터 검증 내용]
1. 관리자 답변으로 완화되는 문제

사진 속 답변의 핵심은 다음이야.

같은 시나리오 내 이전/이후 시간대 feature를 참조해 결측치를 보간하는 것은 이 대회에서 leakage로 판단하지 않는다.
test.csv를 보는 시점에는 해당 시나리오의 관측이 이미 모두 끝난 이후이며, 25개 행이 시간 순서대로 정렬된 연속 스냅샷으로 제공된다.

따라서 보수적으로 봐도, 아래 항목들은 규칙 위반 가능성이 크게 낮아졌다고 판단할 수 있다.

기존에 문제로 본 항목	보수적 재판단
같은 scenario_id 전체 평균/최대/최소/std 사용	대회 기준에서는 허용 가능성이 높음
같은 scenario_id 전체 rank 사용	허용 가능성이 높음
bfill()로 같은 시나리오 내 뒤 시점 값을 앞 결측치에 사용	허용 가능성이 높음
25개 타임슬롯을 하나의 시계열로 Transformer 등에 입력	허용 가능성이 높음
같은 시나리오 내부에서 결측치를 전체 평균으로 보간	허용 가능성이 높음

대회 설명에도 각 시나리오는 약 6시간, 25개 타임슬롯으로 구성된다고 되어 있고, 참가자는 현재 시점의 창고 운영 스냅샷으로 향후 30분 지연을 예측한다고 되어 있다.
즉, 원래 문서만 보면 실시간 예측처럼 해석될 여지가 있었지만, 사진 속 답변은 test 시점에는 한 시나리오의 25개 row가 모두 주어진 상태라고 해석하는 쪽에 힘을 준다.

2. 그래도 조심해야 하는 이유

보수적으로는 이렇게 봐야 한다.

관리자 답변은 **“같은 시나리오 내부 feature 참조”**에 대한 답변이지, 아래까지 모두 허용한다고 확정한 것은 아니다.

target 값을 이용한 feature 생성
test target 추정값을 다시 학습에 사용하는 pseudo-labeling
train/test를 합쳐 encoder나 통계량을 fit하는 행위
test 전체 분포를 이용해 학습 파라미터를 정하는 행위
재현 불가능한 HPO/랜덤성
제출 파일 형식 오류

특히 rules.txt에는 test 데이터는 어떤 형태로도 모델 학습에 활용할 수 없다고 되어 있다.
따라서 같은 test 시나리오 내부 feature 가공은 허용될 가능성이 높지만, train/test를 합쳐서 무언가를 “fit”하는 방식은 여전히 조심해야 한다.
"""